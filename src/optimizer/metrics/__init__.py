"""Nielsen KSA eTAM metric definitions (pure functions) and display rounding."""
from .nielsen import (
    COMMON_WEIGHT_RULES,
    DEFAULT_RATE_DURATION_FACTORS,
    ReachFromAggregatesError,
    average_daily_reach,
    average_daily_trp_abs,
    average_daily_trp_pct,
    average_universe,
    average_weekly_reach,
    average_weekly_trp_abs,
    average_weekly_trp_pct,
    common_weight_rule_from_config,
    common_weights,
    completion_rate,
    cost_per_rating_pct,
    cpm,
    cume_reach_rf,
    cume_reach_rf_from_increments,
    duplication_cume_reach,
    eq_factor,
    exclusive_cume_reach,
    exposure_counts,
    exposures_from_items,
    frequency,
    frequency_from_totals,
    grp_abs,
    grp_abs_from_exposures,
    grp_pct,
    incremental_reach,
    loyalty_pct,
    ots,
    profile_index,
    profile_pct,
    rating_abs,
    rating_abs_combine,
    rating_abs_from_break_trp,
    rating_pct,
    reach_from_aggregates,
    reach_n,
    reach_n_plus,
    reach_pct,
    share_of_audience,
    share_to_selected,
    total_duration,
    trp_abs,
    trp_pct,
    tsv_universe_daily,
    tsv_universe_weekly,
    tsv_viewers_daily,
    tsv_viewers_weekly,
    unduplicated_reach,
    universe,
    weighted_cost_per_rating_pct,
    weighted_cpm,
    weighted_grp_abs,
    weighted_grp_pct,
    weighted_spot_cost,
)
from .rounding import display_round, round_half_up, truncate

__all__ = [name for name in dir() if not name.startswith("_")]
